# Only entrypoint.sh is needed in the build context; exclude everything else
# so docs, compose, the cross-build helper, and exported image tars are not
# uploaded to the daemon on every build.
README.md
docker-compose.yml
build-arm64.sh
*.tar
.git
